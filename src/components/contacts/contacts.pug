mixin contacts
  section.contacts
    div
      each contact, index in contacts.list
        if index > 0
          != ", "
        a(href=contact.link rel="noopener noreferrer" target="_blank")= contact.name
